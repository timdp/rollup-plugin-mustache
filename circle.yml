machine:
  environment:
    NPM_CONFIG_PROGRESS: 'false'
  node:
    version: 6
test:
  pre:
    - mkdir -p $CIRCLE_TEST_REPORTS/unit
  override:
    - npm run test:lint
    - MOCHA_FILE=$CIRCLE_TEST_REPORTS/unit/node6.xml npm run test:unit
